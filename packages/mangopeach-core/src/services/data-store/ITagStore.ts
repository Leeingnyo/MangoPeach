/**
 * Interface for Tag management will be defined here in the future.
 *
 * import { Tag } from '../../models/Tag';
 *
 * export interface ITagStore {
 *   getAllTags(): Promise<Tag[]>;
 *   createTag(tagData: Omit<Tag, 'id'>): Promise<Tag>;
 *   // ... other methods
 * }
 */
