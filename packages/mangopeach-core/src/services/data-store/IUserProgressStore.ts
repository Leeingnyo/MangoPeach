/**
 * Interface for user progress management will be defined here in the future.
 *
 * import { Progress } from '../../models/Progress';
 *
 * export interface IUserProgressStore {
 *   getProgress(userId: string, bundleId: string): Promise<Progress | null>;
 *   updateProgress(userId: string, bundleId: string, page: number): Promise<Progress>;
 *   // ... other methods
 * }
 */
